# remove.bg API Key (required)
# Get it from https://remove.bg/api
REMOVE_BG_API_KEY=your_api_key_here

# API URL (optional, defaults to /api/remove-bg for local development)
# For production with separate Worker:
# NEXT_PUBLIC_API_URL=https://your-worker.workers.dev
